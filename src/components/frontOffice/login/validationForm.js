export function validateName(value) {
  let error;
  if (!value) {
    error = 'Name is required';
  } else if (value.toLowerCase() !== 'aziz') {
    error = 'your name should be aziz';
  }
  return error;
}
// check condition
export function validateEmail(value) {
  let error;
  if (!value) {
    error = 'email';
  } else if (value.toLowerCase() !== 'aziz') {
    error = 'your name should be aziz';
  }
  return error;
}
export function validatePassword(value) {
  let error;
  if (!value) {
    error = 'email';
  } else if (value.toLowerCase() !== 'aziz') {
    error = 'your name should be aziz';
  }
  return error;
}

export function validateSecondePassword(value) {
  let error;
  if (!value) {
    error = 'email';
  } else if (value.toLowerCase() !== 'aziz') {
    error = 'your name should be aziz';
  }
  return error;
}
export function validatePhone(value) {
  let error;
  if (!value) {
    error = 'email';
  } else if (value.toLowerCase() !== 'aziz') {
    error = 'your name should be aziz';
  }
  return error;
}
export function validateString(value) {
  let error;
  if (!value) {
    error = 'email';
  } else if (value.toLowerCase() !== 'aziz') {
    error = 'your name should be aziz';
  }
  return error;
}
export function validateNumber(value) {
  let error;
  if (!value) {
    error = 'email';
  } else if (value.toLowerCase() !== 'aziz') {
    error = 'your name should be aziz';
  }
  return error;
}
